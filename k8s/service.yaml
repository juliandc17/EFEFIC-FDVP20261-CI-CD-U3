apiVersion: v1
kind: Service
metadata:
  name: devops-app
  namespace: devops-lab
  labels:
    app: devops-app
spec:
  type: NodePort
  selector:
    app: devops-app
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
